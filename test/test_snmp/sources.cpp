// Исходники библиотеки включаются в сборку теста напрямую: при проверке на
// машине разработчика PlatformIO не собирает корень проекта как библиотеку
// Arduino.
//
// Сама отправка (FmtSnmp.cpp) сюда не входит: она требует WiFi. Проверяется
// то, что от сети не зависит, - кодирование пакета и его содержимое.
#include <Fmt.cpp>   // из .pio/libdeps, путь даёт build_flags
#include "../../src/FmtBer.cpp"
#include "../../src/FmtSnmpTrap.cpp"
#include "../../src/Log.cpp"
